[server]
headless = true

[browser]
gatherUsageStats = false

[theme]
# Default light theme - dark mode is handled dynamically via CSS in app.py
base = "light"
primaryColor = "#2563EB"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F8FAFC"
textColor = "#1E293B"
font = "sans serif"
